# Legacy Levels

These files use an older level format and are kept only as reference material.
The current runtime loader does not read this directory.

Playable level JSON files live in `Resources/data/`.
